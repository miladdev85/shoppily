export const baseUrl = process.env.REACT_APP_BASE_URL;
export const sections = `${baseUrl}/sectionlinks`;
export const products = `${baseUrl}/products`;
export const mainSections = `${baseUrl}/mainsections`;

export const sectionId = {
  accessoarer: process.env.ACCESSOARER_ID,
  jackor: process.env.JACKOR_ID,
  skor: process.env.SKOR_ID,
  herr: process.env.HERR_ID,
  dam: process.env.DAM_ID
};
